$kind: http-request
name: Save Impact Metrics - Invalid Body
description: |-
  Ensure PUT /admin/impact-metrics rejects a malformed body. Validation runs
  before any database write, so this does not mutate stored metrics.
method: PUT
url: '{{baseUrl}}/admin/impact-metrics'
order: 2601
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "metrics": "not-an-array"
    }
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("PUT /admin/impact-metrics rejects a malformed body", function () {
          pm.response.to.have.status(400);
      });

      pm.test("Response surfaces a validation error", function () {
          const body = pm.response.json();
          pm.expect(body.error || body.message).to.be.a("string").and.not.empty;
      });
